# 007 — M1 coding: Dialogue runner, Debug HUD, and system scaffolding

Coding-expert landed the M1 system scaffolds: `DialogueResource` schema with `DialogueLine` records (no branching, linear only); `DialogueRunner` script and placeholder scene that consumes `.tres` files with typewriter effect at ~40 chars-per-second, suppresses speaker labels for nameless speakers per §11/§10/§29, advances on input (Space or Enter), and emits `line_started`, `line_completed`, `dialogue_finished` signals; a two-line test dialogue at `godot/resources/dialogue/_test.tres` (Nala says "Still here?", a nameless voice replies "Always." to prove the label-hiding); `DebugHUD` script and scene that subscribes to `MortalityClock` (rendering current mortality value, tinting on `mortality_low` and `mortality_exhausted`) and `LedgerState` (rendering sin/redemption counts, flashing the counter row on each event); and debug hotkeys wired into `scene_a`: F1 drains 30% mortality to cross thresholds, F2 records a sin event, F3 records a redemption event, F4 plays the test dialogue. All debug code is marked temporary and will be excised when proper triggers (rescue, dialogue choices, combat) exist. Input actions registered in `project.godot`. Verify by opening `godot/project.godot` in Godot 4.6.2, pressing F5 to boot into `scene_a`, then pressing F1/F2/F3/F4 to trigger the respective systems; mortality should bleed and change color, ledger counters should flash and increment, dialogue should typewriter and advance on Space. Out of scope: UI raster art (ui-ux-expert M1 visual pass), audio (elevenlabs-sfx-expert M2+), branching dialogue (M2+), character-create (M2). Next move: ui-ux-expert polishes layout, typography, and glyph spec; coding-expert awaits M2 dispatch for character-create integration.
