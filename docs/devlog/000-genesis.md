# 000 — Genesis

The repository was bootstrapped on 2026-04-28 with the design bible already
in place. This entry records the scaffolding that landed alongside it:
`CLAUDE.md` (read-first directive), `README.md`, `SETUP.md`, `.gitignore`,
`.gitattributes`, the seven specialist agent prompts under
`.claude/agents/`, the three asset manifests under `assets/`, and the
folder semantics note at `docs/design-notes/folder-semantics.md`. The
trilogy structure is fixed at three parts (Incontinence, Violence, Fraud).
The tonal rules from §23.8 are now referenced from CLAUDE.md and from each
agent prompt — they govern code, prose, and UI copy alike. No game logic
exists yet; the Godot project under `godot/` is a placeholder waiting for
the `coding-expert` to land the first scene. Next moves belong to the
lead PM: sequence the Part 1 vertical slice and decide which beat of the
prologue the team blocks out first. The first open question in §24.5 —
the default-highlighted Class at the Limbo Stone — is on the lead PM's
desk; it is a marketing call as much as a design call.
